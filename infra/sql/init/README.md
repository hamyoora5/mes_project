# Init SQL

이 디렉토리는 초기 SQL이나 수동 시드 스크립트를 두는 위치입니다.

현재 프로젝트는 JPA `ddl-auto=update`와 애플리케이션 시작 시 샘플 시드 로직으로
기본 데이터를 만들고 있어서 별도 SQL 파일이 아직 필요하지 않습니다.

하지만 다음 상황에서는 이 경로가 필요해집니다.
- 로컬 DB를 한 번에 초기화해야 할 때
- 대용량 샘플 데이터를 SQL로 넣고 싶을 때
- 운영/테스트 환경에서 JPA 자동 생성 대신 명시적 스키마를 쓰고 싶을 때
