# Docker Notes

이 디렉토리는 Docker 관련 보조 문서를 두는 위치입니다.

현재 실제 실행 파일은 루트의 `docker-compose.yml` 하나로 충분하므로,
복잡한 설정 파일을 여기로 분리하지는 않았습니다. 다만 프로젝트가 커지면
백엔드, 프론트, DB, 프록시 설정을 개별 Dockerfile과 compose override로
쪼개게 되므로 그 시점에 이 디렉토리를 확장하는 기준점으로 사용합니다.
